services:
  pendo-dev:
    build: .
    container_name: pendo-dev
    restart: unless-stopped
    env_file: .env
    environment:
      - DATABASE_URL=postgresql://pendo:pendo_secret@pendo-dev-db:5432/pendo_dev
      - FLASK_APP=run.py
      - FLASK_ENV=development
    volumes:
      - ./firebase_service_account.json:/app/firebase_service_account.json:ro
    depends_on:
      pendo-dev-db:
        condition: service_healthy
    networks:
      - traefik-public
      - pendo-dev-internal
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik-public"
      # HTTP -> HTTPS redirect
      - "traefik.http.routers.pendo-dev-http.entrypoints=http"
      - "traefik.http.routers.pendo-dev-http.rule=Host(`pendo-dev.ruthwestlimited.com`)"
      - "traefik.http.routers.pendo-dev-http.middlewares=https-redirect"
      # HTTPS
      - "traefik.http.routers.pendo-dev-https.entrypoints=https"
      - "traefik.http.routers.pendo-dev-https.rule=Host(`pendo-dev.ruthwestlimited.com`)"
      - "traefik.http.routers.pendo-dev-https.tls=true"
      - "traefik.http.routers.pendo-dev-https.tls.certresolver=le"
      - "traefik.http.routers.pendo-dev-https.service=pendo-dev-service"
      # Service port
      - "traefik.http.services.pendo-dev-service.loadbalancer.server.port=5000"

  pendo-dev-db:
    image: postgres:16-alpine
    container_name: pendo-dev-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: pendo
      POSTGRES_PASSWORD: pendo_secret
      POSTGRES_DB: pendo_dev
    volumes:
      - pendo-dev-db-data:/var/lib/postgresql/data
    networks:
      - pendo-dev-internal
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pendo -d pendo_dev"]
      interval: 5s
      timeout: 5s
      retries: 5

  pendo-dev-seed:
    build: .
    container_name: pendo-dev-seed
    env_file: .env
    environment:
      - DATABASE_URL=postgresql://pendo:pendo_secret@pendo-dev-db:5432/pendo_dev
      - FLASK_APP=run.py
      - FLASK_ENV=development
    volumes:
      - ./firebase_service_account.json:/app/firebase_service_account.json:ro
    depends_on:
      pendo-dev-db:
        condition: service_healthy
    networks:
      - pendo-dev-internal
    command: >
      sh -c "flask db upgrade && python seed_test_users.py"
    restart: "no"

volumes:
  pendo-dev-db-data:

networks:
  traefik-public:
    external: true
  pendo-dev-internal:
    driver: bridge
